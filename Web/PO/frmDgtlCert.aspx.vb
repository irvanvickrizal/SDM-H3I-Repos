Imports System.IO

Partial Class frmDgtlCert
    Inherits System.Web.UI.Page
    'Public Sub Download(ByVal sender As Object, ByVal e As System.EventArgs)
    '    Dim strPath As String = "cosign.zip"
    '    Dim fpath As String = ConfigurationManager.AppSettings("Fpath") & strPath
    '    DownloadFile(fpath, True)
    'End Sub
    'Private Sub DownloadFile(ByVal filename As String, ByVal forcedownload As Boolean)
    '    If forcedownload Then
    '        Response.AppendHeader("content-disposition", "attachment; filename=" + filename)
    '    End If
    'End Sub
End Class
